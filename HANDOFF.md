# 引き継ぎ — ぐろちゃんシューティング ビジュアル大幅強化

## プロジェクト概要
- 単一ファイル HTML5 canvas 横スクロールシューティング。**`game.js`（~2950行・単一IIFE・ビルドなし）** が本体。`index.html` を直接ブラウザで開くだけで動く。
- アート方針: **ネオン×カワイイを維持したまま、陰影・多層シェーディング・アニメ・エフェクトを大幅強化して立体的・映画的に**。写実路線にはしない。カワイイ目は廃止しない。
- 制約: **新規ライブラリ・外部画像の追加禁止。全て手続き描画。**
- 実装プラン全文: `/Users/daisuke/.claude/plans/optimized-chasing-micali.md`（Phase 1〜6）。
- ブランチ: `agent/remove-unused-warden-sprite`（mainではない）。

## 検証手段（重要）
- **スクショハーネスを `.devtools/` に永続化済み**（puppeteer-core + システムChrome使用、node_modulesは既存）:
  - `node .devtools/shot.js <play|boss|mid> <stageSkips>` → `.devtools/shot-<mode>-s<skips>.png`
    - stageSkips: 0=SHIBUYA 1=AQUA 2=FACTORY 3=STORM 4=PALACE
    - 例: `node .devtools/shot.js play 3`（storm＋敵＋自弾）, `node .devtools/shot.js boss 0`, `node .devtools/shot.js mid 2`
  - `node .devtools/crop.js <png> <cx> <cy> <cw> <ch>` → `.devtools/crop-out.png`（3倍ズーム。敵は小さいので必須）
  - ハマりどころ（対策済み）: headlessはページが隠れ扱いで**ゲームが自動ポーズする**→ `document.hidden` をspoof。敵が出るまで `GRO_DEBUG` をポーリング。自弾を出すにはSpaceを押しっぱなしにする。
- **注意: headless(swiftshader)のFPSは当てにならない。** ≥58fps判定は必ず実機ブラウザで F1 を使ってユーザーが確認する。
- デバッグキー: **F1**=FPS表示トグル / **Shift+N**=ステージスキップ / **Shift+M**=中ボス召喚(戦闘中に2度押しで即撃破) / **Shift+B**=ボス / **Shift+T**=タイムライン+30秒早送り(中ボス/ボス発動の直前で停止)。`window.GRO_DEBUG` で状態取得(`phaseId`/`stageTime`/`continuesLeft`含む)。

## 完了済み（本セッション8コミット、`4b5ece5` の上に積んである）
1. `1629d14` Phase1: `rctx`/`bakeSprite`/`blit` 基盤導入＋葉ヘルパー(drawBox3D/Cylinder/KawaiiEyes/EnemyShadow)を`rctx`化。**bakeSpriteは未使用（基盤のみ）、`rctx===ctx`常時成立で見た目不変。**
2. `aa4a64e` Phase1: F1 FPSカウンタ（`fpsShow`トグル/`fpsAvg` EMA、draw()末尾で表示）。
3. `5540fcf` Phase1: 全画面グラデを`cachedGrad`でメモ化、`nearProps`で毎フレームfilter撤去。
4. `27ca901` Phase2: **Stage1ボス(HEART BREAKER)＋中ボス**を角丸グロス3Dに刷新。箱型「枠」撤去、光沢バイザー、ハート目、3Dポッド/脚。共有ドロップシャドウも角丸化。
5. `ec759ec` Phase4: **storm背景**を全面改修。緑drawCity使い回しを撤去し `drawDataSpires`（黒曜石モノリス＋雷同期回路シーム）/`drawHoloGrid`/`drawWireRings` を新設。
6. `aabd67c` Phase2: `drawBox3D`/`drawCylinder3D` に光沢＋リム＋接地影 → 箱系メカ敵を一括立体化。
7. `9b028d5` Phase2: **drone/tank/turret/walker** に固有パーツ（回転ローター/連装砲＋転動キャタピラ/追尾砲身＋レーダーフィン/2関節脚＋肩キャノン）。
8. `ef4c4fb` Phase3: **自弾**に速度方向コメットトレイル＋星ヘッド、**volt弾**を電撃スパイク、**default敵弾**にコメット尾。`b.seed ??=` 遅延付与。

## 残タスク（ユーザー承認済み・優先順は「背景→敵→弾は着手済、残りを継続」）
- [x] **factory背景の深化**（`drawFactoryBackdrop` ~1592行）: `drawRefineryTanks`（リベット＋発光窓の精錬タンク3基、屋根越しに突出配置）、`drawHammerPress`（周期スラム＋着弾フラッシュのハイドロプレス、gearと同じ「浮遊」扱いで固定スクリーン座標）、`drawMoltenRiver`（コンベア下の流動溶岩グロー）を追加。`REFINERY_TANKS`定数追加。
- [x] **ボス2〜5の磨き**（`drawBoss` の `stageIndex===1..4`）: 共有ヘルパー`drawVisorPanel`(角丸グラデ+ハイライト)/`drawGlowDot`(加算グローの発光瞳)を新設し、DEEP BLUE DIVA/BLAZE EMPRESS/VOLT PHANTOM/QUEEN OF HEARTBREAKの平板な黒fillRect目を角丸グロスバイザー＋発光瞳に刷新（最終ボスはハート型瞳でstage1と統一感）。
- [x] **着弾FX/破片/ヒットストップ**（Phase3後半）: `hitStop`変数を追加し`frame()`で`dt*=.15`のスロー処理（`collisions()`の被弾/`destroyEnemy()`/`hurt()`から`Math.max`でセット、大きさは雑魚<タンク<中ボス<ボスの順）。`burstDebris()`を新設し、角ばった`shape:'shard'`パーティクル（回転しながら落下する破片）を`burst()`の丸グロー粒子と併用。自弾ヒット時は小チップ2枚、撃破時は雑魚7〜ボス26枚。
- [x] **二次アニメ**（Phase2の残り）: `spawnEnemy`/リロード時に`e.fireMax`を記録し、`drawEnemyVariant`で発射直前25%区間にパルスする加算グロー(windup)を追加。同関数に被弾ダメージ状態（HP40%未満でクラック線+明滅エンバー）も追加。`drawKawaiiEyes`に座標シードの非同期瞬き（3.4秒周期・0.1秒だけ閉眼線）を追加し、call site変更なしで全カワイイ目の敵に適用。※`e.recoil`は既にtank砲身が参照、turret砲身は既にプレイヤー追尾。
- [x] **中ボス5種差別化**: 各`stages[]`に`midBoss`名を追加（NEON/TIDAL/CINDER/GLITCH/VELVET WARDEN）、HUDの固定文字列`'CRIMSON WARDEN'`を`stage.midBoss`参照に置換。`drawMidBoss`のクレスト部をステージ別シルエット（ハート型アンテナ/ドーサルフィン/火炎スパイク/サーキット触角+点滅ノード/ハートクレスト）に分岐して差別化。
- [x] **Phase5 残り**: `stageBanner`のタイトルカードを角丸グロー枠+四隅アクセントティック+スライドインに刷新。`drawEnemyVariant`に全雑魚共通のステージ色デカール（右下の小バッジ、accent2地+accentドット）を追加。
- [ ] **Phase6 性能調整**（`bakeSprite`活性化）: **意図的に見送り**。敵の見た目は`e.t`ベースのロータースピン/明滅LED/recoil/windup/damage-crack/瞬き/ステージデカールなど大半が動的で、キャッシュキーに全部含めると焼き込みの意味がなくなる。現状ヘッドレスFPSは指標にならず実機での劣化報告も無いため、リスクに見合わないと判断。実機でF1計測してカクつきがあれば再検討。

## 2026-07-18 セッション: 5分ステージ/コンティニュー/背景立体化
- **ステージ約5分化**: `PHASE_TEMPLATE`(`stages`直後)で waiting 時間226秒(normal)をフェーズ駆動でスクリプト化(trickle→assault→formation→calm→中ボス→calm→assault→setpiece→calm→eliteRush→finalPush)。`difficulties`の`bossTime`は`timeScale`(easy1.06/normal1/hard.92)に置換。`currentPhase(stageTime)`はステートレス導出でデバッグジャンプと両立。敵速度は`stageTime`直結を廃し`rank`(120秒で飽和×フェーズintensity)に、`gameSpeed`もintensity駆動+イージング。セットピースは`runSetpiece`(ダブルVee/地上列+航空/上下ピンサー)、タイムボーナス基準は`timelineTotal()+120`。
- **コンティニュー**: `continuesLeft`(3回)・HP0で`doContinue()`=その場復活(HP全快/無敵4s/敵弾消去/shockwave/スコア維持)。HUD右パネル下にハート3、復活時中央に「CONTINUE! のこりN回」。`resetGame()`でリセット。
- **背景立体化フレームワーク**: `bgCam`(プレイヤー高度追従)+`bgLayer(depth,fn)`で縦パララックス(遠景.5/中景.32/近景.15/前景-.4、地面レイヤーとfactoryのgear/hammerはスクリーン固定のため対象外)。`drawDepthHaze`(遠中間の大気ウォッシュ)、`drawGroundPlane`(消失点VW/2に収束する床グリッド、neon/palaceで使用)、`drawForeground`(drawGame後に描くy>660のテーマ別シルエット帯: ガードレール/波頭/パイプ手すり/ケーブルトレイ/バラ垣)。ステージ別: neon超遠景スカイライン3段/aqua第2島列/factory奥タンク列(`drawRefineryTanks(stage,scale,alpha,shiftX)`)/storm第3スパイア列/palace中間柱廊(`drawColonnade(scale,alpha,speed)`)。
- 検証済み: 全5ステージスクショ、コンティニュー3回→GAME OVER遷移、全フェーズ進行(Shift+T/M/B walk)。実機FPS(F1)は未計測 — ユーザー確認待ち。
- **難易度調整(ユーザーFB反映)**: ボス/中ボスHP約1.6倍+ボスHP25%未満で攻撃サイクル35%高速化(`rageMul`)。高intensityフェーズ(assault/formation)で`spawnFlanker()`=雑魚が左(背後)から回り込み右へ抜ける(`e.flank`、描画は左右反転、除去条件は`x < VW+170`)。SPEEDアイテム強化(+32%加速/+28%最高速 per Lv)。

## 2026-07-18 セッション2: パワーダウン/タイトル画面
- **被弾パワーダウン**: `hurt()`で`player.power`が1段階ダウン(下限1、spread/speedは維持)。`powerDownBanner`(1.4s)でプレイヤー頭上に上昇する「POWER DOWN」表示+HUDのPOWERラベル赤点滅。スタート画面の操作説明に「HIT 被弾でPOWERが1段階ダウン」を追記。`GRO_DEBUG`に`power`を追加。
- **タイトル画面**: canvas描画に移行。`update()`冒頭のmenuブランチで`elapsed`を進め(resetGameで0リセットされるので安全)、渋谷背景をアニメさせつつぐろちゃんがロゴ下を8の字飛行(`drawPlayer`をmenuでも描画)。`drawTitleScreen()`(draw()内、menu時のみ): 上部暗ウォッシュ+「GRO-CHAN」ピンクグラデ+グロー、「SKY BLASTER」シアン、両脇パルスハート、瞬き星8個、PRESS ENTER点滅。HTML `#startScreen`はh1/eyebrow撤去、下部アンカーの透過パネル(難易度/操作/START/ハイスコア)に縮小。
- 検証: `.devtools/shot-title.js`(新規、menu状態スクショ)+`shot.js play 0`で回帰なし確認。
- **メニューフロー3段階化**: タイトル(`#titleScreen`=canvasロゴ+クリック/ENTERヒント+ハイスコアのみ)→説明(`#startScreen`=HOW TO PLAY: 操作2列グリッド/難易度/GAME START、背景ほぼ不透過)→オープニング。`menuStep`('title'/'howto')で分岐、ENTER/クリック/パッドAで進む、ESCでタイトルに戻る。howto中はcanvasのロゴ/ぐろちゃん描画を停止。`shot.js`は`#titleEnter`→`#startButton`→`#launchButton`の順にクリックするよう更新、`shot-title.js`は`title|howto`引数対応。

## 2026-07-18 セッション2 続き: BGM二重再生修正/円システム/ステージ間ショップ
- **BGM二重再生修正**: opening/stage0が同曲(Neon Arcade Rush)を別Audioで持ち、オープニング→ゲーム開始で頭から流れ直していた。1つのAudio要素を共有し、`playBgm`に`previous === next`のシームレス継続分岐を追加(restartは同キー時のみ)。説明画面表示(最初のジェスチャ)で`playBgm('opening')`開始→説明→オープニング→ステージ1まで1曲が連続再生。`showHowto`はバブリング二重発火ガード追加。
- **スコア→円**: 表示を`yen()`(`¥`+3桁区切り)に統一(HUD「MONEY」/リザルト「かせいだ金額」/タイトル「さいこう金額」)。内部変数は`score`のまま(獲得ロジック不変)、ショップで消費可能に。
- **ステージ間ショップ**: クリア遷移(`bossState==='transition'`)完了時に`openShop()`→`state='shop'`(update()停止=休憩)。`#shopScreen`で購入: おにぎり¥3,000(HP全快)/パワー¥8,000/ワイド¥8,000/スピード¥5,000(各Lv3上限)/ハート¥12,000(コンティニュー+1)。`shopItems[]`が`can/apply/status`を持ち`updateShop()`で所持金・上限に応じてdisabled/MAX表示。「つぎのステージへ」ボタン/ENTER/パッドAで`leaveShop()`=旧ステージ前進処理(stageIndex++等)を実行。最終ステージ後は従来どおり即`finishGame(true)`。
- ハーネス: `shot.js`のShift+Nスキップループがショップを`#nextStageButton`クリックで通過、`shop`モード追加(`node .devtools/shot.js shop 0`)。検証: ショップUI表示(金欠disabled/MAX表示)、ショップ経由のstage0→1進行、HUD「MONEY ¥976」表示。購入ボタンの実クリックは実機確認推奨。
- **経済リバランス(ユーザーFB「単位が多い/釣り合っていない/クリアで回復しない・基本はアイテム」)**: 賞金を1/10に圧縮(撃破=`points*mult*diff/10`≈¥12〜76、グレイズ¥4、アイテム取得¥50、ノーダメ¥500、タイムボーナス×5、全クリア¥2,500)。localStorageキーを`grochan-money-best`に変更(旧レコードはインフレ値のため破棄)。ショップ価格: おにぎり¥400(**全回復→HP+40**、ドロップ食品+32と同格)/パワー・ワイド¥1,500/スピード¥1,000/ハート¥2,500。**自動回復を全廃**: ボス撃破+40・中ボス撃破+22・ステージ移行+28を削除、回復は道中ドロップとおにぎり購入のみ(コンティニュー全快は維持)。

## 技術メモ
- 描画はワールド座標(VW=1280×VH=720)。`draw()`でビュー変換を1回適用。ボス/敵は`ctx`にライブ描画（`translate(e.x,e.y)`）。`rctx===ctx`（焼き込み未活性）。
- ヘルパー: `hexA(hex,a)`, `shade(hex,f)`, `heartPath(cx,cy,s)`(ctx使用), `starPath`, `drawBox3D/drawCylinder3D/drawKawaiiEyes`(rctx使用), `clamp`。`ctx.roundRect`は利用可。
- 光源は左上（`drawMoon`と一致）で統一するとリムライトが揃う。
- 敵ペインタ: `drawEnemy` 内の if/else 連鎖（~2140行〜）。ボス: `drawBoss`(~2470行)、中ボス: `drawMidBoss`(~2380行)。
- ステージ定義配列 `stages`（~118行）に sky/far/city/accent/accent2/spawnTable/theme。
- 未追跡: `.DS_Store`（各所）と `node_modules/`。`.devtools/*.png`（スクショ）も生成物。`.gitignore` 未整備（ユーザーに確認保留中）。

## 次アクションの推奨
factory背景 → 着弾FX/ヒットストップ → ボス2〜5磨き → 中ボス5差別化、の順。各項目ごとに `.devtools/shot.js` で検証しコミット。方向性・カワイイ度は前セッションでユーザーOK。

## 2026-07-19 セッション: ストーリーシステム＋AI生成アート一式(codex経由)
- **アセット生成パイプライン**: 3人の三面図(Grok_3view.png等、プロジェクト直下)を元に、codex CLI(`codex exec -m gpt-5.6-sol -c model_reasoning_effort=medium`、プロンプトはstdin渡し必須)で画像生成。絵柄統一のため`assets/images/chibi/grochan_chibi.png`を毎回参照画像に添付。生成スクリプト群はセッションscratchpadに(使い捨て)。
- **ちびドット三面図**: `assets/images/chibi/`に3人分(grochan/chappy/kuroko)。全生成の絵柄アンカー。
- **ストーリー挿絵13枚**: `assets/images/story/`(op1-4/int1-4/ed1-4/go1)。op4/int1/int2は「機首必ず右向き+白いSpaceX風ユニット搭乗」で作り直し済(右スクロールと向きを一致させる)。ed4はイーロン後ろ姿シルエット(顔NG)。
- **ボス三面図6体**: `assets/images/bosses/`(stage1-5+warden)。ふざけデザイン(テープ補修/頭にタコ/たい焼き/グリッチ吹き出し/輪ゴム仮面+ハートいれ段ボール)。stage3(真紅ショート)とstage5(銀白縦ロール)は「主人公と顔が似すぎ」FBで髪色変更再生成済。
- **ストーリーUI**(`#storyScreen`): 額縁スタイル=黒背景+二重枠の挿絵ウィンドウ+下にテキストボックス(DotGothic16)。背景はCSSアニメのサイバー演出(ネオングリッド床スクロール+光ストリーク+走査線)。タイプライター表示(55ms/字、枠は全文サイズで固定=不可視の残り文字を敷く方式、打鍵中▌点滅、1クリック目で全文表示/2クリック目でページ送り)。`STORY`定数(game.js、stages直後)にスライド定義。フロー: オープニング4枚→ミッションカード / 各ステージクリア後に幕間1枚(`state='story'`でupdate停止)→ショップ / 全クリアでed1-3→カメオ画面(顔グラはed4画像) / ゲームオーバーでgo1→リザルト。ENTER/Space/クリックで進行、`cancelStory()`をresetGameで呼ぶ。
- **ボススプライト組み込み**: 三面図の中央1/3(側面ビュー、左向き)をpuppeteer+canvasで切り出し→エッジflood-fillで白背景透過→トリム→`assets/images/bosses/sprites/stageN_side.png`+`warden_side.png`。`drawBoss`/`drawMidBoss`冒頭でスプライト描画(230×190/158×132の作画ボックスにcontainフィット、ステージaccentグロー+浮遊ボブ)、未ロード時は旧プロシージャル描画にフォールバック。抽出スクリプトはscratchpad/extract.js(再利用可)。
- 検証: puppeteer-core+システムChromeでtitle→story→launch→Shift+N×4→Shift+Bの通し確認、全ステージボス表示OK・JSエラーなし。
- 未着手: ボス戦闘挙動は旧来のまま(見た目のみ差し替え)。WARDENの色替え5兄弟化、ボス2形態(女王)、幕間の会話劇化(顔グラ会話)など。

## 2026-07-19 セッション終了時の状態(セッション切替のため記録)
### 完了(コミット済み: e5ae41d まで)
- ストーリーUI/挿絵13枚/タイプライター/サイバー背景/ステージ別地面/ボス3ポーズ/テストモード(?boss=N / ?mid=N)

### 完了(未コミット! 次セッションで動作確認後コミット推奨)
- **ボス総入れ替え**: ユーザー自作の5シート(プロジェクト直下のChatGPT Image 7月18日*.png→`assets/images/bosses/sheets/`にリネームコピー済)を分割し、
  ステージボス1-4を MASQUERADE(仮面道化)/SERVER GOLEM/INFERNO DJINN(炎上魔人)/BOT GENERAL に置換。ステージ5ボスはQUEENのまま。
  旧生成ボス(HEART BREAKER/DIVA/EMPRESS/PHANTOM)は各ステージ中ボスに降格、ステージ5中ボスは LORD CENSOR(検閲卿)。
  WARDEN・女の子中ボス(STELLA等mid1-5)は不使用(mid1素材はposesに残存、削除可)
- ポーズシステム拡張: `loadSet`/`bossSets`/`midSets`(game.js)。シート素材は構え2/攻撃3/被弾2フレームで、
  構え2fアニメ・攻撃は攻撃毎にローテ(attackIdx)・被弾2f交互。ボス高さ560px/中ボス340px。
  ポーズ間でサイズが変わらないようidle基準のpx/スケール統一済
- シート分割: scratchpad/slice_sheets.js(市松背景の焼き込み除去+行列投影分割、行構成[2,3,2]前提)

### 未着手・中断中のタスク
1. **ボスボイス+重厚SFX(中断)**: 効果音ラボの割当案=盗賊の少年(→今は中ボスHEART BREAKER)/幼い王女(DIVA)/ネクロマンサー老婆(EMPRESS)/冷静な魔術師(PHANTOM)/高飛車な魔女(QUEEN)。
   ※ボス入替後なので割当は要再考(新ボス4体は男性的)。mp3直リンクは403→Referer付きcurlを試す(`-H "Referer: https://soundeffect-lab.info/"`)。
   重厚SFX候補: battle/mp3/dragon-cry1, earth-tremor1, super-arts-hit1, building-collapse1
2. 幕間ストーリー文言: 「ハート泥棒の女王・四天王」設定のまま。新ボス(SNS風刺ヴィラン)を物語にどう組むか未定
3. 追加ステージ検討(ユーザー要望あり、1ステージ実プレイ約3.5分/全クリア約20分と伝達済み)
4. ローカルサーバー: python3 http.server 8123 が起動しっぱなし(害なし、`lsof -ti:8123 | xargs kill`で停止)

## 2026-07-19 セッション: SNS風刺ヴィランに合わせた幕間改稿

### 完了
- **幕間ストーリー4枚を新ボス設定へ改稿**: MASQUERADE=ハートを「いいね」に変えて集める仮面の道化、SERVER GOLEM=盗んだハートをため込むサーバー、INFERNO DJINN=怒りや悲しみで巨大化する炎上魔人、BOT GENERAL=女王の命令で動く自動投稿軍団として、各撃破後の文言を一本のSNS風刺ストーリーに接続。既存挿絵と最終決戦への流れは維持。
- **`.devtools/shot.js` のストーリー対応を修復**: 存在しなくなった `#titleEnter` を `#titleScreen` に変更し、オープニング4枚・ステージ間幕間1枚のタイプライター送りを自動化。ストーリー導入後も `boss/shop` モードとステージスキップが実際の対象画面へ到達するようにした。

### 検証
- `node --check game.js` / `node --check .devtools/shot.js` / `git diff --check` 成功。
- ローカルサーバーの `?boss=1..5` を各約10秒待って確認。全5件で `bossState=active`、ボス敵1体、ページJSエラーなし（初回のみ無害な `favicon.ico` 404）。
- `node .devtools/shot.js boss 0..4` を実行し、5ボス・背景・HUDの表示崩れなしを目視確認。
- 通常のステージクリア遷移で幕間4枚を表示し、全文一致・全画面表示を確認。全4枚でテキスト/キャプションとも `scrollHeight === clientHeight`（オーバーフローなし）。

### 現在の残タスク
1. 追加ステージ検討（1ステージ実プレイ約3.5分／全クリア約20分）。

## 2026-07-19 セッション: ぐろちゃんの地上歩行感を改善

### 完了（未コミット）
- 地上の4コマ歩行を固定テンポから移動距離同期に変更。低速時の足滑りを抑え、移動速度に合った足運びにした。
- 歩行中に3pxの小さな上下動、足元パフ、高速時の短いモーションラインを追加。
- 「射撃中なら停止中でも歩行」の条件を廃止。停止射撃はアイドル姿勢を維持する。

### 検証
- `node --check game.js` / `git diff --check` 成功。
- Chromeハーネスで着地後に右移動を入れ、4枚の連続フレームで異なる歩行ポーズ・上下動・足元エフェクトを目視確認。停止射撃でアイドル姿勢へ戻ること、ページJSエラー0件も確認。

## 2026-07-19 セッション: ステージ2〜5背景の作り込み

### 完了（未コミット）
- **AQUA HIGHWAY**: 湾岸高層ビル群、発光窓、観覧車、コンテナクレーンを追加。吊り橋を二脚主塔に作り直し、画面全体に主ケーブルと垂直ハンガーを展開。
- **SUNSET FACTORY**: 両端に巨大高炉、環状キャットウォーク、煙突と動的なガスフレアを追加。
- **CYBER STORM**: 画面中央に破断した発光リング、旋回データ粒子、コア光で構成した巨大データ竜巻を追加。落雷時のサージに連動。
- **HEART PALACE**: 大型ステンドグラス風の薔薇窓、中央ハート、両側の王冠風吊り照明を追加。
- 新規画像・外部アセット・依存は追加せず、Canvas 2Dの手続き描画のみで実装。

### 検証
- `node --check game.js` / `git diff --check` 成功。
- `.devtools/shot.js boss 1..4` でステージ2〜5をChrome撮影。追加景観、背景奥行き、ボス・弾・HUDの視認性を目視確認。

## 2026-07-19 セッション: ステージ直行テストモード

### 完了（未コミット）
- URLクエリ `?stage=N` （N=1..5）を追加。タイトル・オープニングを飛ばし、指定ステージの通常開始地点からHP/強化/タイムラインを初期状態で開始する。
- 既存の `?boss=N` / `?mid=N` と同じ初期化ブロックに統合。複数指定時は `boss > mid > stage` の優先順。
- `AGENTS.md` のテストモード一覧に `?stage=N` を追記。

### 検証
- Chromeで `?stage=1..5` をそれぞれ直接開き、全5件で `state=playing` / `bossState=waiting` / 対応する `stageIndex` / `phaseId=opening` / 通常雑魚スポーン / JSエラー0件を確認。
- `node --check game.js` / `git diff --check` 成功。

## 2026-07-19 セッション: 透明化済み敵シートから全派生画像を再生成

### 完了（未コミット）
- ユーザーが背景を切り抜いて上書きした `assets/images/bosses/sheets/` の5スプライトシートを再分割。各シートから構え2・攻撃3・被弾2の合7枚、計35枚を `poses/` へ再生成。
- 透明化済み三面図6枚（旧ボス5体＋WARDEN）の中央側面ビューを再抽出し、`sprites/stage1_side.png` 〜 `stage5_side.png` と `warden_side.png` を更新。
- 透過画素の行列投影から行[2,3,2]の分割境界を自動検出し、各フレームを透明余白なしでトリム。旧出力と同じ寸法規則を維持。
- ユーザーが削除した `assets/images/refs/mid1_star.png` 〜 `mid5_moon.png` は派生生成に不要のため変更せず、削除状態を保持。

### 検証
- 再生成した代表ポーズ6枚を原寸目視し、背景色・市松模様の残りがないことを確認。
- `.devtools/shot.js boss 0..4` / `mid 0..4` で全5ボス＋5中ボスをChrome撮影。全10体で透明境界、表示サイズ、切り抜け、戦闘画面の視認性に問題なし。

## 2026-07-19 セッション: ゲームオーバーのブラックアウト

### 完了（未コミット）
- コンティニューを使い切って最終的にゲームオーバーになった場合、`.45s` で戦闘画面を完全な黒へフェードする `#gameOverBlackout` を追加。
- 暗転完了後は、従来のサイバーグリッド／光ストリーク付き不時着ストーリー画面を表示。最終リザルトは純黒背景に統一。
- オールクリアエンディングには暗転を適用せず、リトライ時に暗転クラスとレイヤーを確実に解除。

### 検証
- Chromeで暗転完了、ゲームオーバー挿絵、リザルトの3状態を撮影。暗転とリザルトは `rgb(0, 0, 0)`、挿絵画面はサイバーグリッド演出に復帰すること、解除後の復帰、JSエラー0件を確認。
- `node --check game.js` / `git diff --check` 成功。

## 2026-07-19 セッション: ゲームオーバー専用BGM

### 完了（未コミット）
- ユーザー提供の `Game Over, Again.mp3` を `assets/bgm/` に配置。
- ゲームオーバー突入時に戦闘BGMから専用曲へクロスフェードし、不時着ストーリーから最終リザルトまでループ再生するよう追加。
- サウンドOFFから再びONにした場合も、ゲームオーバー中は専用曲へ正しく復帰するよう `desiredBgmKey()` を更新。
- `THIRD_PARTY_ASSETS.md` にユーザー提供素材として記録。

### 検証
- ChromeでBGMを実デコード・再生し、長さ12.28秒、音量0.28、ループ有効、再生時間の進行、JSエラー0件を確認。
- `node --check game.js` / `git diff --check` 成功。

## 2026-07-19 セッション: 全5ステージ背景の生活感・主役景観を追加

### 完了（未コミット）
- **TOKYO MIDNIGHT**: 店舗群の奥を走る3両編成のネオン通勤列車、高架桁、支柱、窓光、レール反射を追加。
- **AQUA HIGHWAY**: 上下線を異なる速度で流れる自動運転車、ヘッドライト／テールライト、路面に伸びる光跡を追加。
- **SUNSET FACTORY**: 工場全体をつなぐ圧力配管、継ぎ手、針が揺れる圧力計、周期的な蒸気噴出を追加。
- **CYBER STORM**: 空間を横断する3本の曲線データ航路と追走する発光パケットを追加。落雷サージに同期して明るさが変化。
- **HEART PALACE**: 薔薇窓と回廊の消失点に、ハート型背板・ベルベット座面・独立して揺らぐ燭台を備えた玉座を追加。
- 新規アセット・依存は追加せず、Canvas 2Dの手続き描画のみ。全要素を背景パスに置き、敵・弾より手前には出さない構成。

### 検証
- `node --check game.js` / `git diff --check` 成功。
- Chromeで `?stage=1..5` を各4秒以上動作させて撮影。全5ステージでページJSエラー0件、追加景観と敵の視認性を目視確認。
- 位置調整後にステージ1〜3を各6秒以上再撮影し、高架列車・湾岸交通・圧力配管が地面レイヤーに隠れず描画されることを確認。

## 2026-07-23 セッション: ステージ2〜5背景の擬似3Dリアル化（最終ステージ最重点）

### 完了（未コミット）
- **共通擬似3D基盤**（game.js の drawVolumeBox 直前に追加、既存描画は無改造）:
  - `proj3(x,y,z)`/`quad3`/`bgLayerZ`: FOCAL=900・HORIZON_Y=560 の真の透視投影。bgLayer(depth) の連続版として camK(z) で視差が深度連動。
  - `fogMix(hex,z)`: 大気遠近（ステージ別 FOG_D、100px刻みでメモ化）。`faceLit(hex,normal,boost)`: ステージ別光源の4法線簡易ランバート（メモ化）。
  - `volPush/volFlush`: Zソート描画キュー（使用はステージ5のみ）。`bgQuality()`: fpsAvg連動の品質3段階（2/1/0）。low時は追加要素をほぼ全て落として従来相当の負荷に戻る。
  - `mixHexA(a,b,t,alpha)`: 色補間ヘルパー。
- **AQUA HIGHWAY**: drawOcean を透視スウェル帯（y→z逆算で地平線に密集、クレスト逆斜面に accent スペキュラ）に全面改修。drawAquaGround の手前の海にも大うねり4帯＋光条。bgLayerZ(2100) の遠景第二橋。橋の桁下×字トラス・橋脚4本＋drawWaterStreak による灯台/橋塔/橋脚の水面光条反射。巨大魚に水面追従シャドウ（drawBigFishShadow）と着水拡散リング（aquaRings、splashRipple からスタガ生成）。
- **SUNSET FACTORY**: 夕日ゴッドレイ（drawSunGodRays、本数は品質連動 6/4/0）。煙突の円筒シェーディング＋楕円キャップ＋太陽側リムライト。クレーン完全シルエット化＋熱リム。煙 ambient を逆光3ロブ（暗紫ボディ＋太陽向き warm rim 弧）＋高度連動の風シアーに。フレア上空の陽炎（品質2のみ）。溶鉱川に遠近流線4帯、spark の発生源を川（y672+）に変更。bgLayerZ(1500) の遠景煙突列。
- **CYBER STORM**: bakeSprite 初活用。積乱雲スラブ4種を焼成し3深度ランクで漂流（bgLayer .5/.32/.22）、最前ランクは落雷近傍で雲内発光（additive グロー＋薄い再ブリット）。drawStormGround を「転がる雲海」に（ベイク済み雲頂ストリップ2列＋うねり＋落雷時のクレスト照り返し）。稲妻を strokeBolt に分離して再帰分岐（子枝2本）＋残光ゴースト（boltGhosts、1.1秒でフェード）。雨は落雷時に明滅。
- **HEART PALACE（最重点）**: drawPalaceBackdrop を volPush/volFlush の深度キューに再構成。
  - 尖頭アーチのステンドグラス窓3種を bakeSprite（鉛桟・宝石ガラス・ハート紋章）→4箇所に配置＋グロー。
  - 窓から身廊中央へ落ちる god rays（平和時ピンク→ボス戦深紅→瀕死血赤、mixHexA 補間、本数は品質連動 4/2/0）。光条内を漂う金の塵埃 ambient 'dust'。
  - 黄金シャンデリア2基（天井ピボットの振り子、二層の腕＋蝋燭＋グロー）。ボス戦中は蝋燭減光、瀕死で揺れ約3倍。
  - drawPalaceVolume をリブヴォールト天井に（暗紫ウェブ＋faceLit 金リブ、瀕死時も金は輝き続ける）。
  - drawColonnade を円柱化（横グラデ大理石＋溝彫り＋金の柱頭/柱礎）。
  - drawPalaceGround に消失点収束の大理石市松（3行スクロール）＋バラ窓の色光プール＋シャンデリア金グリント。
  - バラ窓はボス戦中に心拍パルス。ハート ambient は回転付き花びら挙動、瀕死で黒赤化。
  - ボス演出は `palaceBossMix`（0..1 イージング、resetGame でリセット）1変数で補間。
- 追加ライブラリ・アセットなし。全て手続き Canvas 2D。弾レーンの追加要素は低alpha維持。

### 検証
- `node --check game.js` 成功。全5ステージ play/boss スクショでJSエラー0件・敵弾視認性を目視確認（ステージ1は無改修で回帰なし）。
- ステージ2: 巨大魚ジャンプ＋水面シャドウを連写で確認。ステージ4: 稲妻（分岐・全画面フラッシュ・雲海照り返し）を輝度サンプリングで捕捉確認（約4〜5秒周期で発火）。
- ステージ5: ボス戦で背景が戦闘照明に遷移すること、女王・弾の視認性を確認。
- **未確認**: 実機FPS（headless不可のため）。実機ブラウザ+F1 で ≥58fps の確認はユーザーに依頼。瀕死（bossCrit）演出はパラメータ変更のみのため実プレイでの目視推奨。

## 2026-07-23 セッション2: 全ステージに「本物の透視投影ボリューム」を追加（最終ステージ最豪華化）

### 完了（未コミット）
- **共通基盤の拡張**（volFlush 直後）:
  - `boxZ(x0,x1,yTop,yBase,z0,z1,front,side,top,alpha)`: proj3 による真の透視押し出しボックス。可視側面→天面/底面→前面の順で描画し、前面スクリーン矩形（同一zの4隅は必ず軸平行矩形）を返すので、**ベイク済みファサードを blit で貼れる**。
  - `worldXAt(sx, s)`: proj3 の x 逆変換（遠景ランドマークを画面上の構図位置にピン留めする用）。
  - `fogAmount(z)`: ベイク画像に fogMix が使えない場合のフォグ washアルファ係数。
- **TOKYO MIDNIGHT**: `drawNeonCanyon` — 窓グリッド+ネオン看板柱をベイクした `neonTowerSprite`(4種) を、z=950/1900 の2ランクで boxZ に貼る真3Dビル渓谷。ワールドx等速スクロール→投影で近いほど速く流れる。3本に1本、屋上アンテナ+明滅航空ビーコン。品質1でランク1本に減、0で消灯。
- **AQUA HIGHWAY**: `drawAquaTanker` — z=1500 に巨大コンテナ船（傾斜バウの船体 quad3、喫水線ストライプ、コンテナ段積み boxZ、船橋楼+窓灯、ファンネル、明滅マスト灯、バウウェーキ、水面リフレクション）。橋の背後・海の上をほぼ常時ゆっくり左航行（spanW短縮済み）。
- **SUNSET FACTORY**: `drawCoolingTowers` — z=2600・worldXAt でスクリーンx425/872 に固定した双曲面冷却塔（太陽側ホットリム+スロート楕円+風下へ流れる逆光スチーム）。`drawFurnaceRow` — `furnaceShedSprite`（コルゲート壁+白熱炉口）を z=1050 の boxZ 列に貼り、炉口に生フリッカーの加算グロー。**手前の drawCity(600) の後に描画**（描画順で埋もれるため移動済み）。
- **CYBER STORM**: `drawMainframeRow` — `mainframeSprite`(3種、LEDマトリクス) を z=1500 の boxZ 列に貼るサーバーモノリス群。品質2で生LED明滅、落雷近傍でエッジ発光+頂部ビーコン増光。
- **HEART PALACE（最重点・豪華/荘厳/厳つい）**:
  - `drawColonnade` 撤去 → `drawPalaceNave(stage, z, alpha, dressed)`: `palaceColumnSprite`（溝彫り薔薇大理石+金の二層柱頭/柱礎）を z=620/1280 の2ランク boxZ 列に。隣接柱頭間に金のアーチ（暗色太帯+faceLit金線)。dressedランクは `palaceBannerSprite`(ハート紋/王冠紋の2種、金トリム+フリンジ) がアーチ中点から揺れて垂れ、柱基部に燭台トーチ（ボス戦中は減光）。柱基部は y=652 の床先端ラインにピン留め（`yBase = HORIZON_Y + 92/s`）。
  - `drawPalaceStatues`: `palaceStatueSprite`（王冠+光輪+茨の翼+胸のハートジェム+大剣を床に突き立てた黄金の守護巨像、200×430ベイク）を左右 x188/1092 に2体。**drawPalaceVolume のリブ天井の後に描画**（ヴォールトの暗リブ柱と完全に重なり埋もれるため。volPush だと不可視になる）。加算再blit(.26)で金のブルーム、`palaceBossMix`>0 でバイザーが赤く燃える+瀕死で明滅。
  - 薔薇窓: 金のサンバースト棘トレサリー(24本)+真珠ボス(12個)+頂部に5尖塔の王冠フィニアル(ジュエル付き)。両脇 x±340 に小薔薇窓2枚（8スポーク+ハート芯）。
  - 床: 各ステンドグラス直下に色光の鏡面スミア（ボス戦で深紅化）、巨像直下に金のスミア、床を流れる金の煌めきダイヤ6個。
  - 前景: palace枠ピラーに金のフィレット+ハート鋲。
- 追加ライブラリ・アセットなし。全て手続き Canvas 2D + bakeSprite。新規要素は全て bgQuality() 3段階でゲート。

### 検証
- `node --check game.js` 成功、全ショットでページJSエラー0件。
- `shot.js play 0..4` / `boss 1,2,4` / `mid 1,2,4` で全5ステージ撮影し目視確認（渓谷/船（右端航行中）/シェッド炉口+冷却塔/モノリスLED/宮殿フル装備+巨像+赤眼）。
- スプライト単体検証: scratchpad の statue-test.js（bakeSprite スタブ+puppeteer）で巨像ベイクを原寸確認。
- **注意**: `.devtools/crop.js` の cx,cy は**左上基準**（中心ではない）。
- **未確認**: 実機FPS（F1）。ボス撃破瀕死時の巨像明滅は実プレイ目視推奨。

## 2026-07-24 セッション: ゲームプレイ大型更新（弾数制/必殺技/障害物/敵強化/演出/サントラ特典）

計画全文: `/Users/daisuke/.claude/plans/compiled-orbiting-zephyr.md`。4コミットに分割（a1f1ea4 / 821d5a3 / 5c3431b / 本コミット）。

### 弾数制（A1）
- 通常ショットに弾数: `ammo/ammoMax`、`difficulties[].ammo`（easy220/normal170/hard140）、`shoot()`1ボレー=1消費。**弾切れ時は`shootPea()`（dmg.6・rate.28s・無限）へフォールバック=ソフトロック不可**。ミサイルは消費なし。
- HUD: SPECIAL直下にAMMOバー（残25%で赤点滅+数値）。弾切れ瞬間に「弾切れ! AMMO EMPTY」上昇タグ（`ammoBanner`）。
- 補充: `doContinue()`全快 / `leaveShop()`で最低60% / 弾薬クレート+40（不足するほどドロップ率↑=`.10+.25*不足率`）/ ショップ弾薬パック¥900で+120 / ブロック破壊14%でammo/heal。

### コマンド必殺技（A2）
- 8方向`motionBuf`（テンキー表記、方向変化時のみpush、0.45s窓、`checkMotion(seq)`は順序部分一致）。fire押下エッジ（`wasFiring`）で`tryCommandMove()`→通常射撃より先に判定。
- **↓↘→+ショット「ハートウェーブ」**（ゲージ35%）: 貫通巨大ハート（`pierce:true`、敵ごとに`e.pierceCd`=0.3sでヒット、hue循環描画）。
- **→↓↘+ショット「スパークライザー」**（ゲージ40%）: 上昇voltピラー10発+半径200px敵弾消去+無敵0.6s。
- 2つのシーケンス(2,3,6 / 6,2,3)は相互排他。X=100%全画面ボムは従来どおり。ゲージ不足はSEのみ。**タッチ操作では発動不可（既知の制限）**。HOW TO PLAYに追記済み。

### 破壊可能障害物（A3）
- `spawnBlockWall()`: 92px角×6セルの縦壁（ギャップ easy2/hard1セル、`gapW`で判定）を`enemies`配列の`type:'block'`として生成 → 弾衝突/接触/コンボ/賞金が既存経路で動く。hp `3+stageIndex`、points90、vx130。
- 出現: stage2以降のassault中12%ロール（`blockWallTimer`6sクールダウン）+セットピースpattern4（壁+護衛3体）。
- 接触は専用分岐: `hurt(18)`+セルhp-4（体当たりで削れる）。`drawBlock`はテーマ別（看板ビル/錆コンテナ/耐熱ブロック/データキューブ/薔薇レンガ）+HP比率クラック2段階。
- 連鎖爆発の巻き添え対象（意図的、爽快感）。

### 敵/ボス/中ボス強化（A4）
- `spawnFormation`に新shape: `wall`（縦一列5体）/`snake`（behavior'snake'=スロット位相ずれの共有サイン蛇行）/`rows`（上下2列スタッガー）。stage1は旧shape70%。`runSetpiece`は5パターン（+スネークチェーン、+ブロック壁）。
- ボス広ビーム（既存beam hazard流用）: idx0「クロスシザー」`bossCrossBeam`（対角±.35radの2本）、idx2「ヒートプレス」`bossHeatBeam`（プレイヤー行に回廊を残す上下2本）。どちらもtier1+の発動プール1/3。`TEL_LABEL`追加済み。
- 中ボス: HP×1.12、扇/リング弾数に`barrage`係数、新攻撃ミニビーム（`e.beamT`6.5s周期/怒り5s、aim付きh44/gapW、dmg22）。

### アイテム/ショップ（B）
- 弾薬クレート（`drawAmmoPickup`）とライフカプセル（`drawLifePickup`、maxHP+15）。lifeはstage2+の2.5%枠を`lifeDropsSpawned`で1ラン3個上限+**stage3中ボス撃破で確定1個**。
- ショップ`buyAmmo`追加（8品目=4×2グリッド、style.cssコメント更新）。

### 演出（C）
- POWER3で通常弾がhue循環虹オーブ（`b.hue`シード、白コア+コメット尾は維持=敵弾と判別）。
- 花火: bgProp`firework`（neon/palace各2基）→上昇ストリーク→`fwflash`閃光+`fwspark`グリッター（[12/24/40]粒、bgQuality連動）。palaceで`bossState transition/final`中は0.7-1.7s間隔の祝砲連発。
- 音反応スピーカー（neon、`drawSpeaker`）: BGM低域`musicLevel()`でコーン脈動+ビートリング。**AnalyserNodeはhttp(s)のみ配線**（`ensureBgmAnalyser`、Audio要素ごと1回のMapキャッシュ。file://でcreateMediaElementSourceするとBGM無音化するためプロトコルゲート、失敗時は恒久的にsin疑似ビート）。`playBgm`の2箇所のplay()後に呼ぶ。

### サントラ特典（D）
- ハード全クリアで`localStorage['grochan-hard-clear']='1'`（`finishGame`）。タイトル右下+リザルトカードに`soundtrack.html`リンク（`.is-hidden`トグル、`refreshSoundtrackLinks()`が起動時/クリア時に反映）、エンディングに解放ノート。
- **クロール対策**: soundtrack.htmlは既存の`noindex,nofollow`メタ+リンクに`rel="nofollow"`。**robots.txtには意図的に書かない**（Disallowは公開ファイルでパスを晒す上、クロール禁止だとnoindexメタが読まれない）。

### デバッグ拡張
- `GRO_DEBUG`: ammo/ammoMax/blocks/musicReactive/hardClear追加。
- フック: `__wall()` `__setAmmo(n)` `__setSpecial(n)` `__drop(type)` `__fwCount()` `__bgm()`。URL `?unlock=1|0`。

### 検証
- puppeteerスクリプト（scratchpad）で自動確認: 弾切れ→豆ショット、両必殺技のゲージ消費、壁スポーン/射撃破壊、クロスシザー/ヒートプレスのhazard生成、中ボスミニビーム、ammo/life取得値、花火94粒バースト、http=アナライザ配線+BGM再生・file=非配線+BGM再生、解放フロー6項目（ロック/解放/遷移/ハードクリアでフラグ/ノート表示/ノーマルクリアで非解放）。
- スクショ: AMMOバー、ショップ4×2、耐熱ブロック壁、スネーク隊列、スピーカー+ビートリング、虹弾、タイトルのSOUND TRACKリンク。ページJSエラー0件。
- **注意**: `__setHp(.5)`でtier到達させるとボスがhideリトリートに入りテレグラフが凍結する（テスト時は`__armTelegraph`を直接使う）。
- **未確認**: 実機FPS(F1)と実機での必殺技入力感、弾数エコノミーの体感バランス（数値は全てtunable、計画書に一覧表）。

## 2026-07-25 セッション: ボス/中ボス大幅強化＋ショップに弾薬フルチャージパック

ユーザーFB「ボスの体力をもっと多くして。特に中ボスが弱い。ラスボスも体力が少ない。攻撃も強く派手に」「ショップで弾薬をフルにチャージできるパックを5個まで買えるように」を反映（未コミット→本コミットで反映済み想定）。

### HP強化
- `difficulties[].bossHp/midHp` を大幅増: easy 300→480/90→170、normal 560→900/170→320、hard 800→1300/240→460。
- `spawnBoss()`: ステージ倍率 `.55`→`.65`、**最終ステージ(stageIndex===stages.length-1)は追加で×1.3**（`isFinalBoss`分岐）。normal最終ボス実効HP: 1792→4212。
- `spawnMidBoss()`: 倍率 `1.12`→`1.5`、ステージ倍率 `.38`→`.48`。normal中ボス実効HP: stage1約190→480、stage5約480→1402。中ボスは体感的に「特に弱い」FBに直接対応する伸び幅にした。

### 攻撃強化＋演出
- ボス全攻撃関数（``）のdamageを一律+25〜30%程度、`bossFan`のn弾数・`bossVoltRing`/`bossHeartRing`/`bossRoseLattice`の基礎弾数を増加、`burst()`/`shake`を各所に追加または増量して着弾/発射がより派手に。クイーンの`bossHeartSpiral`アーム数をtier2で4→5、tier1で3（新規）に。
- 中ボス（`updateMidBoss`）: 扇弾16+idx→21+idx*2、リング弾15+idx→19+idx*2、側面キャノン14+idx→18+idx*2（4発/側に増加）、ミニビームdmg22→28・幅44→50px・周期短縮、`burst`量増加+shake追加。
- 全て`hurt()`の`difficulties.damage`乗算は既存のまま（データフローは変更なし、raw値のみ底上げ）。

### ショップ「フルチャージパック」（新規ストック型アイテム）
- 新変数 `ammoPackStock`(0〜5)・`reloadFlash`(バナータイマー)。`charmStock`と同じ「買ってストックし、条件成立時に自動発動」パターン。
- 発動条件: 発射ゲートで`ammo<=0 && ammoPackStock>0`になった瞬間、**豆ショットに落ちる前に**1個消費して`ammo=ammoMax`まで即時全回復（`burst`+`shockwave`+SE+「フルリロード！」上昇バナー）。在庫が尽きれば従来どおり豆ショットにフォールバック（ソフトロック防止は維持）。
- ショップ項目 `buyAmmoPack`（¥1,200固定、`ammoPackStock<5`でボタン活性、状態表示「のこり N/5」）を`shopItems`に追加、`index.html`にボタン追加。
- **ショップUIを4×2(8品目)→3×3(9品目)グリッドに再編**（`style.css` `.shop-items` の `grid-template-columns` を `repeat(4,...)`→`repeat(3, minmax(140px,190px))`に変更）。配置: 1行目パワー/ワイド/スピード、2行目おにぎり/弾薬パック(+120)/フルチャージパック(新)、3行目おまもり/ハート/ドリンク。
- HUD: AMMOパネル右上に小さく「PACK×N」バッジ（`ammoPackStock>0`時のみ、SPECIAL/AMMOバーやラベルと重ならない位置に配置済み）。
- デバッグ: `GRO_DEBUG.ammoPackStock`、`window.__grantAmmoPack()`、`window.__bossMaxHp()`（ボス/中ボスの`maxHp`を直接取得、HP検証専用）追加。

### 検証
- `__bossMaxHp()`でnormal難易度の実効maxHpを直接確認: boss stage1=900 stage5=4212、mid stage1=480 stage5=1402（すべて設計値どおり）。
- ショップ購入フロー（`__grant`で所持金付与→`__openShop`→`#buyAmmoPack`クリック）でストック+1を確認。
- 弾切れ時のオート補充: `__setAmmo(2)`+`__grantAmmoPack()`×2→撃ち切り後 `ammoPackStock`が1減り`ammo`が全快することを確認、豆ショットに落ちないことも確認。
- クロスシザー等の既存ボス広ビームが引き続き発火することを回帰確認（`__armTelegraph`）。
- スクショ: ショップ3×3グリッド、ステージ1ボス/中ボス（新damage/burst量の弾幕）、最終ボス（HP4212表示・見た目回帰なし）、AMMOパネルのPACKバッジ配置。全てページJSエラー0件。
- **未確認**: 実機での難易度体感（HPを大きく上げたため、ラスボス撃破までの実プレイ時間が伸びている可能性。ユーザーに実プレイでの確認を推奨）。

## 2026-07-25 セッション2: ステージ1の「雑魚の物量」だけを緩和

ユーザーFB「ステージ1はもう少し難易度を優しくしたい」→追加FB「**ボスの強さではなく、ザコキャラの数とか**」を反映。緩和対象を道中の混雑度に限定し、**ボス/中ボスのHP・攻撃力・被ダメージ計算には一切手を入れていない**（ステージ1のボスも他ステージと同じ強さ）。

> 初回実装では`bossHp .72`/`midHp .62`/`hurt()`の`damage .75`も入れていたが、上記FBを受けて**全て撤去済み**。`spawnBoss()`/`spawnMidBoss()`/`hurt()`は元のコードに戻っている。

### 実装
`difficulties`直後に定数 `STAGE1_EASE` と述語 `isStage1 = () => stageIndex === 0` を追加。数値を1か所に集約したので再調整はこのテーブルだけで済む。適用先は全て`isStage1()`ガードなのでステージ2以降のコードパスは不変。

| キー | 値 | 適用先 |
|---|---|---|
| `cap` | 3 | assault/formationの同時出現上限を−3。calm/trickle/setpieceは元の上限が3〜5と小さいため`easeCapLow = min(cap, 1)`で−1に留め、レーンが空にならないようにした |
| `spawnGap` | 1.45 | 全モードの`spawnTimer`に乗算。追加スポーン/フランカーの抽選確率は逆に`/ easeGap`で低下 |
| `packSize` | .6 | `spawnFormation()`と`runSetpiece()`の1回あたりの湧き数。`squad(n) = max(2, round(n*.6))`で下限2（1体だと「隊列」に見えないため） |
| `variant` | .55 | `spawnEnemy()`のelite/armored抽選閾値（elite 11%→6.05%、armored 20%→11%） |
| `fire` | 1.25 | 雑魚の`e.fire -= dt / (difficulty.fireGap * ...)`。ボス/中ボスは`continue`で手前を抜けるため独自クールダウンのまま |

- `spawnFormation()`: ステージ1は`pool`を`['vee','column']`固定に。**大型シェイプ（wall=5体/snake=7体/rows=8体）をステージ1から完全に外したのが物量削減の主因**。vee/columnの人数も`squad()`で3〜4→2に。
- `runSetpiece()`: 全5パターンに`squad()`を適用（ダブルVee 5→3/翼、スネーク 8→5、壁の護衛 3→2、地上列+航空 3+3→2+2、ピンサー 4+4→2+2）。ダブルVeeの`off`は固定`i-2`から`i-(n-1)/2`に直して人数可変でも中心が揃うようにした。

### 検証（puppeteer + `GRO_DEBUG`、normal難易度）
- **ボス/中ボスHPが元に戻っていることを確認**: stage1 boss=900 / mid=480（初回実装の648/298から復帰）。
- 道中前半（opening〜breather1、46秒）の同時出現数: stage1 **平均3.78 / 最大7**（変更前 6.72 / 13）。stage2は8.05 / 15で据え置き。
- setpieceフェーズ単独: stage1 **平均6.9 / 最大11**、stage2 11.6 / 20。
- スクショ比較（formationAフェーズ、同条件6秒後）: stage1=敵2体、stage2=敵14体。ステージ1はレーンがはっきり読める密度になった。
- ページJSエラー0件、`node --check`通過。
- **計測時の注意**:
  - `?mid=N`/`?boss=N`ジャンプは`spawnTimer=999`を入れるので**通常スポーンが止まる**。この経路でassault/finalPushの敵数を測ると0になるが不具合ではない。setpieceだけは`spawnTimer`ゲートの外なので計測可能。
  - AFKボットのHP減少量・画面内敵弾数はRNG分散が大きく（同条件3回で弾数5.5〜12.7、HP減0〜85）指標にならない。安定指標は同時出現数と`__bossMaxHp()`のみ。
- **未確認**: 実機での体感。緩めすぎ／まだ物足りない場合は`STAGE1_EASE`の5数値だけで再調整可能。敵数が減った分ステージ1の稼ぎは落ちるので、1回目のショップの所持金が想定より少ないようなら`points`側で別途調整が要る。

## 2026-07-25 セッション3: 背景の作り込み — 章立て/住人/天候/遠景ランドマーク（未コミット）

ユーザーFB「各ステージの背景をもっともっと作り込みたい」。方向性は本人選択で**4軸すべて×全5ステージを薄く一律に**。テーマ別に個別実装せず、**共通システム＋テーマ別パラメータ表**で5ステージが同時に底上げされる構成にした。

### 最重要の前提: フレームは fill-rate 律速であって JS 律速ではない
着手前にCPUプロファイルを取ったところ、**最重量のパレスでもJSは約91%アイドル**のままフレームは16msかかっていた。つまりボトルネックはピクセル塗り。この事実が設計を決めている:

- **小さいマークを多数足すのはほぼタダ** → 住人・モート・線画ランドマークはこの方針で作った
- **全画面のブレンド/グラデ層は高い** → 新規追加では1枚も足していない（章のwashだけが唯一の全画面fillで、それも単色グラデ1回）
- 実測: 新レイヤーON/OFFの差は **-1.4〜+0.9ms（ノイズ範囲内）**。5ステージすべてp50 11〜13msを維持
- 参考: グラデ生成は全体で152箇所ありcachedGradは15箇所だけだが、**JSがアイドルなので生成コストは問題ではない**。最適化するならfill面積であってグラデ生成ではない

### 実装（すべて `isStage1()` 的なテーマ分岐ではなくテーマ別パラメータ表）
- **章立て `CHAPTERS=3` / `SCENE_CHAPTERS`（軸4）**: `backgroundDirector()` に `chapter` / `chapterT` / `chapterIn` を追加。`stageTime`から導出するステートレス構成は既存方針を踏襲。**ボス・中ボス戦中は最終章にピン留め**（`route`が1を超えて序盤の景色に戻るのを防ぐ）。各章に `wash`(空の色) と `mote`(浮遊物の色) を持たせ、`drawChapterWash()` が上半分に1枚だけグラデを敷く
- **住人 `LIFE`（軸2）**: テーマごとに3種×2〜5体。鳥のスケイン/魚群/エイ/ヘリ/飛行船/貨物ポッド/データ蛾/センサー/蝋燭の魂/鳩/幟。`drawLifeAgent()` に全種の描画を集約、いずれも40px以下
- **天候 `WEATHER`（軸3）**: 1つのモートプールを `far`/`near` の2深度で描き分け。`fall`/`drift` の符号だけで「降る・昇る・横殴り」を表現し、テーマ別の更新コードは不要。shapeは streak/dot/petal
- **遠景ランドマーク `FAR_LANDMARKS`（軸1）**: 章ごとに1セット、計15セット。`farRow()` が横スクロールする列を共通処理。章の変わり目で `chapterIn` によりクロスフェード

### 実装中に判明した2つの落とし穴（重要）
1. **描画順**: 当初は空グラデの直後（テーマ背景の前）に遠景を描いていたが、`drawNeonBackdrop`など複数のテーマ背景が**空帯を不透明で塗り直す**ため完全に消えていた。→ **テーマ背景の後・volumeパスの前**に移動。これで前景ジオメトリによる遮蔽は保たれる
2. **暗いステージではダークシルエットが読めない**: ネオンの巨大タワーは暗紫#2a1358 alpha .34で描いていたが夜空に完全に溶けた。→ **エッジライト（accent色のライン画）で構造を見せる方式**に変更して初めて抜けた。同じ理由でネオン章3は「タワーを遠景に置く」のではなく**橋脚が画面を跨ぎ下部トラスが上端を額装する『タワー直下』**の構図に作り直している

### 検証
- **A/Bトグル `window.__bgLayers(bool)` と `window.__bgDir()` を追加**（新レイヤーだけをON/OFF、ディレクタ状態を直読み）。これがないと既存背景が密すぎて追加分を目視判別できない
- ピクセル差分で効果を定量化: 2フレーム間のモーションノイズをベースラインとして差し引き、**新レイヤーの寄与は正味 7.3〜15.2ポイント**（stage2が最大15.2pt、stage5が最小7.3pt）
- 全5ステージ×全3章のスクショを取得、**ページJSエラー0件**、`node --check`通過
- 計測の注意: `Shift+T`は中ボス手前(49s)で頭打ちなので、章2に到達するには`Shift+M`×2で中ボスをスキップしてから`Shift+T`が必要
- **未確認**: 実機での体感。特に「薄く一律に」の狙いどおりか（`SCENE_CHAPTERS[].washA` と `WEATHER[].a`、各ランドマークの`alpha`が濃さの主ツマミ）。パレス章3の全幅fillは fill-rate 方針に反するため実装途中で撤去済み

## 2026-07-25 セッション4: フルチャージパックを初期3個持ちに（未コミット）

ユーザーFB「最初は弾のストックは3つにして。いまは少なすぎる」。**「弾のストック」がボムかフルチャージパックか曖昧だったため確認し、フルチャージパック／ゲーム開始時のみ**と確定。

- `AMMO_PACK_START = 3` / `AMMO_PACK_MAX = 5` を新設。上限5が「ショップの`can`」「ステータス表示」「`__grantAmmoPack`」の3箇所にベタ書きされていたのを定数へ集約（開始値と上限が今後ずれない）。
- `resetGame()` の `ammoPackStock` を 0→`AMMO_PACK_START`。**コンティニュー時は補充しない**（`doContinue()`は未変更）＝あくまで序盤のクッションで恒久的な安全網にはしない、という意図をコメントに明記。
- 弾切れ前にショップへ到達できないのが序盤の一番きつい所だったため、その入り口だけを緩める狙い。ステージ1の雑魚削減（セッション2）と同じ「序盤だけ」の方針に揃えている。

### 検証（実機ドライブ、normal）
- 開始時 `ammoPackStock` = **3**、HUDのAMMOパネルに「PACK×3」バッジが最初から表示されることをスクショで確認。
- 自動発動: `__setAmmo(1)`から撃ち切り → ストック 3→**2**、`ammo` が 161/170 まで即時回復（豆ショットに落ちない）。
- ショップ上限: 8回連打しても **5でMAX表示**、`AMMO_PACK_MAX`が効いている。
- ページJSエラー0件、`node --check`通過。

## 2026-07-25 セッション6: ビキニ衣装（ショップ解禁＋時間回復）

ユーザーがビキニ版のスプライトを3枚追加。「アイテムでビキニを買うとステージ2から使える／時間とともに少しずつ体力と弾が回復する」。

### 画像の取り込み — そのままでは差し替えられなかった
提供画像は既存シートと**余白もセルのアスペクトも違う**（歩行セル: 既存298x308 に対し 434x725、被弾セル: 627x627）。drawPlayerはセルのアスペクトを固定値で持っているので、素直に差し替えると横に潰れる。
→ `refitStrip()` / `refitBox()` を新設。各セルを実描画部分でトリムし、**元と同じアスペクトの枠へcontainフィット**（下端揃え・水平中央）で焼き直す。これで **drawPlayer側は一切変更せず**両衣装が同じ描画コードで動く。
- トリムは1枚ごとではなく**全フレームの外接矩形（union box）**で行う。1枚ごとだとコマ毎に中央寄せされ歩行アニメがガタつく。
- webp化: `cwebp -q 88 -alpha_q 100`。元PNG3枚はリポジトリ直下に未追跡のまま残っている。

### 落とし穴1: 髪が見切れた（ユーザー報告）
飛行・ジャンプの枠を「列あたりの不透明ピクセル数が多い箇所＝本体」で切り出したため、**数px厚しかない毛先が閾値に届かず落ちていた**（左右で11〜26px欠損）。
→ 本体検出後に**外側の薄い列へ伸長**する方式へ。ただし伸長は**34pxで打ち切る**: マズルフラッシュの筋が約110pxあり、無制限だと隣コマと繋がって枠が345pxまで膨張し、逆にキャラが3割縮む。
→ 飛行3コマは **y/h を共通化**（コマ毎に高さが変わると再生中に伸縮する）。
→ **切り出し後の再トリムを廃止**（`refitBox`）。二重トリムはコマ毎に中心をずらし、アニメをガタつかせるだけだった。
→ 検証用に `__bikiniDump()` を追加し、生成された全15フレームを市松背景に並べて目視した。

### 落とし穴2: 弾が銃口からずれる（ユーザー報告）
`shoot()`の発射座標は元衣装のスプライト前提の固定値。ビキニは**銃を7〜8px高く構えている**ためずれる。
→ 各衣装のフレームを**drawPlayerと同じ矩形に描いて最右不透明ピクセル（=銃身先端）を走査**して実測:
  - fly: normal (118,71) / bikini (116,64)
  - ground: normal (119,64) / bikini (122,56)
→ `MUZZLE_BASE`（既定衣装の銃口）+ `MUZZLE_FIX`（ビキニの差分）に整理。ミサイルも同じ銃から出るので同じ補正。
→ **ついでに既定衣装の地上も是正**: 従来の地上発射点は (114,80) で銃身より16px下だった（ビキニ以前からのずれ）。ユーザー了承のうえ銃口へ合わせた。結果、4パターンすべて**ずれ0**。
- 計測スクリプトの注意: 走査キャンバスに**背景を塗ってはいけない**（全画素alpha255になり「最右の不透明画素」が無意味になる。一度これで誤検出した）。

### ゲーム内容
- `bikiniOwned` / `bikiniOn() = bikiniOwned && stageIndex >= 1`。ショップはステージ1の休憩所に出るので「買った次のステージから」が自然に成立する。
- 回復: `BIKINI_HP_PER_SEC=0.9` / `BIKINI_AMMO_PER_SEC=2.2`。端数をキャリーして整数になった分だけ加算（毎フレーム0.02ずつ動かすとバーが滲むため）。死亡中は回らない。
- ショップ `buyBikini` ¥3,200・買い切り。買い切り品は`can()`がfalseでも「MAX」ではなく自前の文言を出したいので、`keepStatus` フラグを`updateShop()`に追加。状態は みしゅとく → つぎのステージから → そうびちゅう。
- 10品目になり 4/4/2。`.shop-item`のpadding/gapを詰めて 1280x720 の overflow を0に維持。

### テスト用URL（追加）
- `?shop=N` … ステージN直後の休憩＆ショップへ直行（しょじ金+¥30,000）。Nは最終ステージ未満。
- `?bikini=1` … ビキニ所持状態で開始。`?stage=N`/`?shop=N`と併用可。
- デバッグ: `__grantBikini(bool)` / `__bikiniDump()` / `__frameDump()`（両衣装のフレーム配列）。

### 検証
全5ステージで装備状態のスモーク（JSエラー0件）、ステージ1でゲートが効くこと（owned=true/on=false）、回復5秒でHP58→62・弾40→51（対照: 非装備は弾40のまま）、ショップ実購入→次ステージで発動、銃口ずれ4パターンとも0。

## 2026-07-25 セッション5: ショップ4列化＋弾薬まわりの改名

ユーザーFB「４列２行にして」「弾薬は５つまでもてます。できてないと思う」「紛らわしいとこは修正」。

### ショップ4列化＋はみ出し修正
- `.shop-items` を `repeat(3, ...)` → **`repeat(4, minmax(140px,190px))`**。**9品目あるので4列でも2行には収まらず 4/4/1 になる**旨をユーザーに提示したところ「あとでアイテム増やすのでやって」との回答。将来10品目で4/4/2、12品目で綺麗な4×3。
- 余った最後の1つは `:last-child:nth-child(4n+1)` で `grid-column: 2 / span 2` + 中央寄せ。左端に取り残された見た目を防ぐ。
- **はみ出しバグを修正**: 1280×720 で `.shop-screen` が36pxオーバーフローし「つぎのステージへ」が切れていた。`.shop-items` の gap 14→10 / margin、`.shop-item` の padding・gap、`.shop-next`・`.shop-lead` の margin を圧縮して **overflow 0**（1280×720 / 1440×900 とも実測確認）。

### 「弾薬パック」と「フルチャージパック」の改名（混乱の実害があった）
ユーザーが両者を取り違えて「5つまで持てるのができていない」と報告。実装は正常だったが**名前が近すぎるのが原因**なので、役割を名前・説明・状態表示の3箇所すべてで言い分ける形に変更:

| id | 旧 | 新 | 説明 | status |
|---|---|---|---|---|
| `buyAmmo` | 弾薬パック | **弾薬チャージ** | その場で弾薬+120（もちものにならない） | `いま N/M` |
| `buyAmmoPack` | フルチャージパック | **スペアマガジン** | もちもの。弾切れの瞬間に自動でフル装填（最大5個） | `もちもの N/5` |

- HUDバッジ `PACK×N` → **`SPARE×N`**、自動発動バナー「フルリロード！」→「**スペアマガジン！**」。
- ロジックは一切変更していない（`buyAmmo`は即時消費、`buyAmmoPack`は`AMMO_PACK_MAX=5`までのストック）。

### 開発サーバのキャッシュ問題（再発防止済み）
ユーザーが「できていない」と感じた直接原因は**ブラウザが古い`game.js`を掴んでいたこと**。`open <URL>`は既存タブがあるとリロードせず前面に出すだけなので、編集が反映されない。`python3 -m http.server`はno-cacheヘッダを出さないためこれが起きる。
→ **`tmp/serve.py`（no-store/no-cache/must-revalidateを付与するSimpleHTTPRequestHandler）を用意**。以後の動作確認はこちらを使うこと。`AGENTS.md`記載の`python3 -m http.server 8123`のままだと同じ事故が起きる。

### 検証
- タイトル→説明→GAME START の実フローで `ammoPackStock=3` を確認（`?stage=N`ジャンプだけでなく通常起動でも初期3個）。
- 実ショップで6回連打: 3→4→5→**5でMAX・disabled**。`AMMO_PACK_MAX`が効いている。
- 弾薬チャージは `ammo < ammoMax` のときのみ購入可（満タン時にMAX表示になるのは仕様どおり）。
- ページJSエラー0件、`node --check`通過。

## 2026-07-24 セッション: 背景リッチ化の追加方針検討

### 状態
- コード変更なし。既存の全5ステージ通常プレイスクショと背景実装を確認。
- 静的な景観密度と擬似3Dの物量はすでに十分高い。次の改善軸は、`PHASE_TEMPLATE`・中ボス・ボス状態に同期して背景が段階的に変化する「背景ディレクション」が最も効果的。
- 優先候補は、各ステージ1つの大型セットピース、道中の4〜6段階の景観変化、ボス戦での背景変貌、光と反射の連動。弾の視認性と `bgQuality()` による負荷制御を維持する。

## 2026-07-24 セッション2: 全5ステージの背景ディレクション実装

### 完了（未コミット）
- `activePhase` / `stageTime` / 中ボス・ボス状態から演出値を毎フレーム導出する `backgroundDirector()` と、全テーマ共通の床面光反射 `drawScenicLightEcho()` を追加。URL直行・Shift+T/M/B・コンティニューでも状態ずれしないステートレス構成。
- **TOKYO MIDNIGHT**: calm/setpiece中のスクランブル群衆、ボス戦の街灯減光、MASQUERADEによる大型広告ジャックを追加。
- **AQUA HIGHWAY**: setpieceで画面を横切る二脚主塔・横梁・主ケーブル・ハンガー、ボス戦の海面ストレージグリッドと発光データ片を追加。
- **SUNSET FACTORY**: 巨大取鍋から溶鉱川への注湯、稼働強度に連動するプレス列、ボス戦の全館減光と炉口の照り返しを追加。
- **CYBER STORM**: 落雷時にだけ姿を現す巨大司令構造体、setpieceのサーバー塔連鎖過負荷、ボス戦の走査グリッチを追加。
- **HEART PALACE**: setpieceで開く儀礼大扉、ボス戦で薔薇窓から成長する亀裂、外側から消える燭台列、瀕死度に連動する深紅の床光を追加。
- `.devtools/shot.js` に `setpiece` モードを追加し、中ボスを通常デバッグ経路で倒して後半セットピースを直接撮影可能にした。
- 新規依存・画像・音声なし。全追加描画はCanvas 2Dで、敵・弾・HUDより後方。装飾本数は既存 `bgQuality()` で段階制御。

### 検証
- `node --check game.js` / `node --check .devtools/shot.js` / `git diff --check` 成功。
- `node .devtools/shot.js setpiece 0..4` で全5面の大型演出を撮影。全件 `phaseId=setpiece`、ページJSエラー0件。敵・弾の視認性と描画順を目視確認。
- `node .devtools/shot.js boss 0..4` で全ボス背景変貌を撮影。全件 `bossState=active`、ページJSエラー0件。
- AQUAの通過塔は初回撮影で一枚板に見えたため、脚幅を縮めて中央開口と三段横梁を追加し、再撮影で橋塔として読めることを確認。
- **未確認**: 実機F1での58fps以上、QUEEN瀕死時（bossCrit最大）の亀裂・床光最終段階。

## 2026-07-26 セッション: ステージ2の深海生物ロスターとプレイヤー描画倍率の統一

### 完了（未コミット）
#### 1. ステージ2 AQUA HIGHWAY 専用の敵4種を追加
これまで全ステージが共通のメカ系ロスター（drone/bat/spinner/racer…）を使い回していたため、
海のステージに海の生き物がいなかった。実在種の解剖学的特徴をそのまま作り込み、
配色と発光だけネオンを維持する方針（**ユーザー直接指示により AGENTS.md の「写実路線にはしない」を
シルエットに限り上書き。カワイイ目は `drawFishEye` として残存**）。

- **`dumbo` メンダコ** (Grimpoteuthis) — ベル型の外套膜、推進に使う大きな耳鰭、8本の腕をつなぐ
  傘状の膜（スカラップ状の裾＋吸盤列＋シリー）、透けて見えるU字内殻。最遅の敵。
  攻撃は狙い撃ちではなく**膨らむ墨の弾幕3発**（`ink` 弾種を新設）。撃破時に墨を吐く。
- **`angler` チョウチンアンコウ** (Melanocetus) — 球形の黒い体、針状の歯、誘引突起と発光する
  エスカ。**エスカの輝きが発射チャージに連動する＝予備動作そのもの**。`lure` 弾を2発。
- **`oarfish` リュウグウノツカイ** (Regalecus glesne) — 実物どおり**頭を上にした縦姿勢**で、
  体はほぼ動かさず深紅の背鰭だけを波打たせて泳ぐ。頭部鰭条の冠、2本の長い腹鰭（オール）。
  レーン内で縦の柱として機能する。
- **`moray` 巨大ウツボ** (Gymnothorax javanicus) — 頭部だけが当たり判定で、胴はボックス右外へ
  流れる。豹柄の斑紋（body pathでclip）、淡い腹側、鼻管。**射撃せず突進で攻撃**し、
  咽頭顎（第二の顎）が喉から射出される。突進→後退→待機の3相（`lunge`/`recoil`/`jaw`）。

関連する変更:
- ステージ2 `spawnTable` を `jelly/manta/dumbo/angler/oarfish/moray/racer` に差し替え（メカ系はracerのみ残置）。
- 敵タイプ分類を `GROUND_TYPES`/`ORGANIC_TYPES`/`SOLO_TYPES` に集約（従来3か所に重複していた地上判定を統合）。
- `SOLO_TYPES`（moray/oarfish）は編隊・フランカー・セットピースから除外。7体並びのウツボは壁でしかない。
- `drawEnemyVariant`: 生物はリベット/王冠/部隊章/装甲のひび割れを描かない。
  eliteは発光ディスプレイ、armoredは背の鱗板、瀕死は裂傷＋生物発光の出血に置換。
- `destroyEnemy`: 生物は金属片ではなく淡い鱗片を散らす。
- 墨弾は水中背景と同系色で見えないため、明るい紫の縁取りを追加（ダメージ13→11）。

#### 2. プレイヤーの空中／地上でのサイズ差を解消
3枚のスプライトシートが別々に描き起こされており、キャラのスケールが揃っていなかった。
- 飛行セル: 132×167 → **150×185**（地上比で約11%小さかった）
- ジャンプセル: 128×175 → **145×189**
- 被弾セル（空中）: 147×152 → **177×183**。地上と**同じ絵を17%小さく**描いていた最大の原因。
- 全数値を `PLAYER_DRAW` に集約。地上セル(177×183)を基準にし、各セル固有のアスペクトは維持、
  `ox`/`oy` は体の中心が動かないよう設定したのでシート切替で跳ねない。
- 追随して `MUZZLE_BASE.fly` を (118,71)→(127,72)、`shootMissile` の空中オフセットを (88,76)→(93,78)、
  スラスター位置と空中の接地リングを再導出（いずれもセル内相対位置 u,v から算出）。

#### 3. デバッグフック追加（`LOCAL_DEV` 内のみ）
- `__spawn(type, x, y, hold=true, over=null)` … 指定タイプを固定位置に出して静止させる。
  `over` でvariant/hp/jaw等を上書きでき、絵柄検証がスポーン運任せにならない。
- `__clearEnemies()` / `__enemies()` … 雑魚一掃 / 敵の軽量スナップショット（挙動プローブ用）。

### 検証
- `node --check game.js` 通過。ページJSエラー0件。
- 4種すべてを `__spawn` で固定配置して拡大目視。初回は「メンダコが傘の見えない団子」「リュウグウノツカイが
  赤い塊」「ウツボがワニ」に見えたため、耳鰭をパドル形に、傘を暗色＋スカラップ裾に、背鰭を頭部より下から
  開始、冠鰭条を先端で扇状に、ウツボは吻を短く深い頭蓋＋暗い口内に作り直して再撮影で解決。
- ステージ2の自然湧きプレイを撮影し、墨弾の視認性と全体の画面成立を確認。
- ウツボの挙動を `__enemies()` で13秒追跡: x=1195→1041→(突進)756→654→(突進)577 と前進しており、
  画面右で停滞しない。突進が当たると接触判定で相打ち死する（全雑魚共通の既存仕様）。
- プレイヤーは実機描画で空中／地上を撮影し、頭・目・髪留め・イヤリングのサイズ一致を目視確認。
  銃口位置も再撮影でマズルフラッシュと弾の発生点が銃身先端に一致することを確認。
- **未確認**: 実機F1での58fps以上（敵1体あたりの描画量は増えている）、ビキニ衣装での銃口位置の再確認、
  ウツボ突進時の咽頭顎ポーズ（ステージ開始バナーで暗転した状態でしか撮れていない）。


### 追記（同セッション）: ウツボを「巨大な魚」として作り直し＋4種の作り込み
ユーザー指示「たまに出現する巨大な魚をもっとリアルに。うつぼでもいい」を受けての第2波。

**ウツボを遭遇スケールに拡大**（`drawGiantMoray()` として独立関数化、約150行）
- 当たり判定 118×64 → **150×92**。HP 10→16、点数 1150→1800、出現x も +260 に。
  頭だけが判定で、胴は約350px ボックス右外へ流れる（全長 ~490px）。
- 追加した実物の特徴: 背鰭〜尾鰭〜臀鰭の連続した鰭リボン（鰭条・淡い縁取り付き）、
  2スケールの斑紋（頭部の細かい斑点が胴で大きな鞍状斑に育つ）、**単一の円形鰓孔**、
  管状の前鼻孔＋後鼻孔、金色の虹彩、長さの階調がある湾曲歯＋口蓋の鋤骨歯列、粘液のツヤ。
- **アイドルの開口は鰓のポンプ動作**（morayは口を開けて鰓に水を送る）。sin波ではなく
  開いて保持して閉じるリズム。
- 進行波は尾に向かって振幅が増え位相が遅れる＝1本の筋肉として泳ぐ。
- 描画中に見つけて直したバグ:
  - 顎の回転符号が逆で、`open` を上げるほど口が閉じていた（ヒンジが後方なので下顎は負回転）。
  - 上顎の腹側縁が下顎の背側縁より下にあり、下顎の歯が頭蓋に埋まって見えなかった。両方 y=0 に統一。
  - 喉の暗色ウェッジが吻より前に飛び出していた＋アイドル時に顎下から赤い帯が漏れていた。
    → 吻より手前で止め、高さを `open` に比例させた。
  - 鰭パスが自己交差して尾に切れ込みが出ていた → 単一ループに書き直し。
  - 頭と胴のグラデーションのワールドY範囲が違い、首に縦の継ぎ目が出ていた → 同一範囲に統一し、
    頭蓋の後端を +44 まで伸ばして重ねた。
- 左端カリングをウツボだけ -560 に（頭が -130 を割った時点でまだ胴が画面内にいるため）。

**残り3種の作り込み**
- メンダコ: 色素胞の明滅（実際に体色が波打つ）、外套膜の乳頭突起、目を大型化。
- チョウチンアンコウ: たるんだ皮膚のひだ、鰓裂、胸鰭の鰭条、エスカの繊毛、誘引突起の肉質化。
- リュウグウノツカイ: 背鰭の個別鰭条（実物は数百本の櫛）、グアニン層の虹色バンド。

**負荷対策**: ウツボの細斑と鰭条は既存の `bgQuality()`（fps由来の0/1/2）で段階的に間引く。
細斑 18/36/54本、鰭条ステップ .9/.55/.3。

**未確認（第2波ぶん）**: 実機F1でのfps（ウツボは通常雑魚の約7倍の描画コール数。画面に2匹出る想定）。


### 追記2（同セッション）: ステージ2の在来2種も実在種に置き換え＋細部
「作り込んで」を受けての第3波。新しい4種の隣で、旧アートのクラゲ・マンタだけ明らかに見劣り
していたのでこの2種も実在種で描き直した。これでステージ2の生物は全6種が実在種になった。

- **`jelly` → クロカムリクラゲ Atolla wyvillei**
  - 冠状体クラゲの名の由来である**傘を一周する深い溝**、20本の短い縁触手＋**1本だけ極端に長い
    引き触手**（本種の identifying feature）、放射管、傘越しに透ける胃。
  - **「泥棒警報」生物発光**を実装。被弾時（`e.hit`）と瀕死時に青いリングが明滅する。
    本種の発光は捕食されたときに発するもので、**この敵が既に撃破時に4方向弾をばら撒く挙動と
    同じイベント**なので、光と攻撃が解剖学的に一致した。
  - 目は縁の**ロパリア（眼点を持つ感覚器）**という扱いで2つだけ残した。
- **`manta` → オニイトマキエイ Mobula birostris**
  - 向きを他の敵と同じ「頭が左」に変更（従来は頭が上）。
  - 本物のマンタを他のエイと分ける3点を実装: **頭部の先端にある終位の口**（イトマキエイ類は
    腹側にある）、遊泳時に巻いている**頭鰭（cephalic fin）**、個体識別に使われる**白い肩の斑紋**。
    加えて尾の付け根の小さな背鰭と鞭状の尾。
  - 羽ばたきは**外側へ伝わる波**にしたので翼端が常に肩より遅れる。上下の翼が同時に縮む＝
    真上から見た羽ばたきの短縮として正しい。

**ウツボの追加細部**
- 金色の虹彩に**放射状の筋**を入れ、**スリット瞳孔がグロちゃんを追う**ようにした
  （`drawFishEye` と同じ挙動。見据えられている感じがウツボの怖さの大半）。
- 頭蓋と下顎のグラデーションを **`ctx.rotate()` の前に生成**するよう修正。回転後に作ると
  グラデーションが顎と一緒に傾き、胴との陰影がずれて首に継ぎ目が出ていた。
- 頭蓋の後端を斜めカットに（垂直な合わせ目は残差があると直線として目立つため）。

**メンダコ/アンコウ/リュウグウノツカイの追加細部**は追記1に記載のとおり。

**未確認**: 実機F1でのfps（この3波で描画量は大きく増えている。特にウツボとクラゲ）。

### 次にやるなら
- 残り4ステージにも同じ手順で固有敵を作る（本セッションはユーザー合意のうえステージ2のみ）。
  渋谷夜＝カラス/野良猫/ネオンの虫、工場＝溶鉱炉の生物、電脳嵐＝雷の生物、宮殿＝宮廷の獣、など。
- `ORGANIC_TYPES` に `bat`/`jelly`/`manta` も入れてあるので、旧来の生物3種も装甲デカールが外れている。
  必要ならこの3種の絵柄も実在種寄りに描き直せる。

## 2026-07-26 セッショ2: 残り4ステージの固有雑魚ロスター

### 完了（未コミット）
- ステージ1 `TOKYO MIDNIGHT`: `crow` ネオンカラス / `alleycat` 路地猫 / `neonmoth` ネオン蛾。
- ステージ3 `SUNSET FACTORY`: `slagling` スラグ生命体 / `rivetbeetle` リベット甲虫 / `furnacehound` 炉心猟犬。
- ステージ4 `CYBER STORM`: `cloudray` 雲エイ / `voltbug` 電光虫 / `packetwyrm` パケット龍。
- ステージ5 `HEART PALACE`: `rosebud` 戦闘薔薇 / `cardguard` トランプ衛兵 / `teacup` 暴走ティーカップ。
- 全12種を各ステージの `spawnTable` へ編入。サイズ・HP・速度・得点・射撃間隔、編隊可否、地上/生物分類を個別調整。
- 全種に専用Canvas 2D描画を追加。翼の羽ばたき、走行脚、ホバー、体節の位相遅れ、発射チャージ発光と左上光源の立体陰影を実装。
- 射撃もキャラ固有化：猫の看板破片、蛾の燐粉弾、溶岩弾、炉心ブレス、雷弾、パケット扇弾、薔薇ハート弾、カード直射、紅茶泡弾。
- `.devtools/shot.js` を localhost 接続に直し、`roster` モードを追加。高ステージの `play` 撮影が前ステージの残HPで即座ポーリング終了する問題も修正。

### 検証
- `node --check game.js` / `node --check .devtools/shot.js` / `git diff --check` 成功。
- `node .devtools/shot.js roster 0,2,3,4` で全12種を固定配置撮影し、シルエット・陰影・ステージ背景との分離を目視確認。
- `play 3/4` で自然湧きを長めに追跡。STORM 13体+敵弖16発、PALACE 10体+敵弑1発の画面で描画崩れ・JS例外なし。
- ハーネスの `404` は favicon 要求による既存のコンソールエラーのみ。新規描画の例外はなし。
- 新規依存・画像・音声アセットなし。実機F1での58fps以上は未確認。

## 2026-07-26 セッショ3: 固有雑魚12種の高密度リデザイン

### 完了（未コミット）
- ユーザー評価「ザコキャラが雑」を受け、セッショ2で追加した12種を造形から描き直し。モチーフが読めるだけの基本図形の組み合わせから、骨格・素材・可動部・顔・攻撃器官が分かれた描画へ高密度化。
- **TOKYO**: カラスは初列風切・胸羽・喉羽・身元タグ、猫は肩/後肢・踵・肉球・毛の縞・首輪札、蛾は前翅/後翅・翅脈・眼状紋・鱗粉毛・櫛状触角を追加。
- **FACTORY**: スラグは冷却玄武岩板・溶岩裂け目・気泡・粘性足、甲虫は6本の関節脚・翅脈・鍛造翅鞘・リベット・大顎、猟犬は煙突・炉心・鉄リブ・通気口・関節脚に分離。
- **STORM**: 雲エイは雲塊の体積・電気毛細管・頭鰭/口/雷尾、電光虫は4枚翅・翅脈・6脚・複眼・腹部体節、パケット龍は背鰭・ヘッダ表示・チェックサム灯・角・髭・開口を追加。
- **PALACE**: 薔薇は3重の花弁渦・萼・葉脈・棘・予張茎、カード衛兵は紙の厚み・隅数字・紋章・腕脚・帽・ハルバード、ティーカップは取っ手の厚み・金彩・薔薇模様・貫入・紅茶渦・泡・茶匙に分離。
- 羽根・翅脈・雲塊・薔薇花弁は `bgQuality()` で描画本数/層数を落とす。密度向上時も既存のFPS適応を維持。

### 検証
- `roster 0/2/3/4` で全12種を再撮影し、固定配置の実表示サイズで部位と素材差が読めることを目視確認。
- `play 0/2/3/4` で自然湧きを再確認。FACTORY 7体/12弾、STORM 12体/10弾、PALACE 13体/16弾で新規JS例外・描画崩れなし。
- 新規依存・画像・音声アセットなし。実機F1で58fps以上は未確認。

## 2026-07-26 セッショ4: ステージ2ボスをABYSS SIRENへ交代

### 完了（未コミット）
- ユーザー提供の透過PNG 2枚（4x2ポーズシート）から、人魚ボス `ABYSS SIREN` の
  idle2 / attack3 / hurt2 を切り出し、同一354x461キャンバスへ下端中央揃え。
  `cwebp -near_lossless 40 -m 6 -alpha_q 100` でWebP化した。
- 攻撃エフェクトが4列グリッドをまたいでいたため、attack1/2/3だけ透明な隙間を境界にした専用範囲で
  再切り出し。手前の爪・魔力弾・岩盤破砕がセル端で見切れないようにした。
- ステージ2ボスだけ描画高を560→640へ拡大。開始Yを30へ上げ、当たり判定の左右insetを22%にして
  大型化した岩座・尾が理不尽な接触判定にならないよう調整。
- 直行テストURLに `?power=1..3` / `?wide=1..3` を追加。
  `?boss=2&power=3&wide=3` で人魚ボス戦を最大火力・最大拡散から確認可能。
- ステージ2の表示名・ボスセット・被弾色・幕間文・ボイスを人魚へ変更。ボイスは既存witch音源を
  playbackRate 0.82で使用。SERVER GOLEMのコードと全素材は削除せず保留。
- イラストの攻撃ポーズと技を固定対応:
  - attack1（爪を伸ばす）=`深淵の爪`: 高速の三連紫爪撃。
  - attack2（両手から魔力弾）=`アビス・オーブ`: 通常3連弾＋低速巨大追尾オーブ。
  - attack3（尾で岩盤を破砕）=`深海テールクラッシュ`: 狙った床の衝撃波＋岩礫噴出。
- 各必殺技中は通常オーブ射撃を短時間止め、攻撃ポーズが別技に上書きされないようにした。
- 水中退避中の旧ゲイザー攻撃を紫の岩礫噴出へ差し替え、復帰時も人魚のオーブ攻撃に変更。
- `THIRD_PARTY_ASSETS.md` にユーザー提供画像、切り出しWebP、利用許諾を追記。

### 検証
- `node --check game.js` 成功。
- `?boss=2` へ直行し、10.5秒待って入場完了後に `__forceAttack()` で claw / abyssorb /
  tailslam を個別撮影。透過・サイズ・ポーズ対応を目視確認し、ページJS例外0件。
- 巨大化・再切り出し後も同じ4画面を再撮影。全身が720px内に収まり、手から出る魔力弾の発光尾まで
  欠けずに表示されることを確認。ページJS例外0件。
- 検証画像: `.devtools/siren-idle.png`, `siren-claw.png`, `siren-abyssorb.png`,
  `siren-tailslam.png`（作業確認用、コミット対象外）。
- 実機F1で58fps以上は未確認。
